export const config = {
    /**
     * Where to put the files
     */
    outputPath: 'generated',
    /**
     * Prefix for generated images
     */
    qrPrefix: 'qr-',
    /**
     * QR Code size in bytes.
     * More bytes - bigger image.
     */
    sliceSize: 1500,
    repoUrl: 'https://github.com/ElForastero/gpgqr',
}
